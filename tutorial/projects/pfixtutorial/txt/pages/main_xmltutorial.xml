<?xml version="1.0" encoding="ISO-8859-1"?>
<include_parts xmlns:ixsl="http://www.w3.org/1999/XSL/Transform" xmlns:pfx="http://www.schlund.de/pustefix/core"><!--Append include parts here...-->
  <part name="content">
    <product name="default">
      <lang name="default">
        <center><h2>Basic Application</h2><br/>               </center> 	
    		<pfx:include part="intro"/>
    		<hr/>
     		<pfx:include part="dirs"/>
    		<hr/>
     		<pfx:include part="zeugs"/>
      </lang>
    </product>
  </part>
  
  <part name="intro">
  	<product name="default">
    	<lang name="default">
    		<h3>Basically ...</h3>
    		<p>The projects/ dir contains the xml/xsl parts of all projects within the pfixtutorial. 
			To keep it simple there is only one project here. There are some directories that
			contain files that are needed by all projects, you can simply copy the 
			bin/, common/, core/ and servletconf/ directories from another project. You can find the complete
			core/ directory in the file pfixcore-data-0.2.x.tar.gz</p>
		</lang>
    </product>
  </part>
  
  <part name="dirs">
  	<product name="default">
    	<lang name="default">
    	<h3>Directories in the projects/ dir</h3>
    	<ul>
    		<li>bin/					some scripts</li>
			<li>common/		stuff that is used by multiple or all projects</li>
			<li>core/				the pustefix core stylesheets</li>
			<li>servletconf/		servlet configuration files</li>
		</ul>
		</lang>
    </product>
  </part>
  
  <part name="zeugs">
  	<product name="default">
    	<lang name="default">
    	<h3>Misc Stuff</h3>
		  <p>The ptutorial/ dir is the directory for the sample project of the tutorial. For further
			information see the tutorial file there.</p>
		  <p>The Makefile in this directory can be copied, you do not have to adapt it!</p>
		  <p>The servletconf/projects.xml has to be adapted to your project. Simply substitute all occurrences of 
			"pfixtutorial" with the name of your project  </p>
		  <p>Do not forget to link the generated apache.conf into your apaches conf.d/ directory 
			otherwise it will not be accessible.</p>
	</lang>
    </product>
  </part>
  
</include_parts>
